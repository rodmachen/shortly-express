
Only shortens valid urls, returning a 404 - Not found for invalid urls
Responds with the short code
New links create a database entry
Fetches the link url title

Returns the same shortened code
Shortcode redirects to correct url
Returns all of the links to display on the links page

Redirects to login page if a user tries to access the main page and is not signed in
Redirects to login page if a user tries to create a link and is not signed in
Redirects to login page if a user tries to see all of the links and is not signed in